#include <iostream>
#include <string>
#include <sstream>
using namespace std;

struct GetAllData {
public:
	string a;
};

struct GetData {
public:
  string stockName;
  string startdate;
  string enddate;
};

//struct GetData = {"VOLV-B.ST",startdate,enddate} volvo;
//struct GetData() OMX;
